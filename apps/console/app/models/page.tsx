import { Screen } from '@/components/layouts/screen';

/** Declared in `packages/ui-metadata/src/layouts/models.ts`. ADR-015; ADR-009 §4. */
export default function ModelsPage() {
  return <Screen manifest="models" />;
}
