/**
 * Next.js Instrumentation Hook
 * MSW browser setup is handled in layout.tsx, not here
 */

export async function register() {
  // Instrumentation is used for server-side code
  // Client-side MSW initialization happens in app/layout.tsx
}
