{
  "name": "@metis/ui-metadata",
  "version": "1.0.0",
  "description": "METIS form descriptors — the metadata registry screens are declared from",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ./vitest.config.ts"
  },
  "devDependencies": {
    "js-yaml": "^5.4.1",
    "vitest": "^1.0.0"
  }
}
