{
  "name": "@metis/keys",
  "version": "1.0.0",
  "description": "METIS key store — per-tenant keys above per-subject keys, and erasure by destroying them (ADR-025)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --config ./vitest.config.ts"
  },
  "dependencies": {
    "@metis/core": "*"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "pg": "^8.23.0",
    "vitest": "^1.0.0"
  }
}
