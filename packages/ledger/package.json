{
  "name": "@metis/ledger",
  "version": "1.0.0",
  "description": "METIS decision ledger — append-only records, outcomes, durable idempotency",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run --config ./vitest.config.ts"
  },
  "dependencies": {
    "@metis/runtime": "*"
  },
  "devDependencies": {
    "@types/pg": "^8.23.1",
    "pg": "^8.23.0",
    "vitest": "^1.0.0"
  }
}
