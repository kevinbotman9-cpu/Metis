{
  "name": "metis",
  "version": "0.0.1",
  "description": "METIS - AI-native decision hub with deterministic execution",
  "private": true,
  "workspaces": [
    "packages/*",
    "planes/*",
    "infrastructure/*",
    "bench/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "test": "jest",
    "lint": "eslint packages planes bench infrastructure",
    "dev": "docker-compose up",
    "compile": "node -r ts-node/register packages/compiler/src/cli.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.40.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
